{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "activitylog.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/apilogs",
      "handler": "apilog.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/apilogs/:id",
      "handler": "apilog.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}
